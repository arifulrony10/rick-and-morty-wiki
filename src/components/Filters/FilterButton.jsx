const FilterButton = ({ buttonName }) => {
    return (
        <div>
            {/* 
            
            <button className='bg-white h-full text-green-900 font-bold py-2 px-4 border-2 border-green-900 rounded hover:bg-green-500  hover:text-white transition ease-in-out hover:ease-in-out delay-150'>
            {buttonName}
        </button>
            */}
        </div>
    );
};
export default FilterButton;
