const Card = ({ character }) => {
  console.log('🚀 ~ file: card.component.jsx:2 ~ Card ~ character:', character);

  const { name, gender, image, status, type, url } = character;

  return <div></div>;
};
export default Card;
